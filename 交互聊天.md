帮我生成git常用指令的练习指导，并以.md格式给出

# 🚀 Git 常用命令实战练习（Trae IDE 版）

本教程通过 **完整动手流程**，带你在 Trae IDE 中练习 Git 常用命令。  
适合初学者，一步一步操作即可掌握 Git 日常开发 80% 的使用场景。

---

## 📌 练习目标

掌握以下核心命令：


init / add / commit / status / log / diff
branch / switch / merge
remote / push / pull / clone
stash / reset / revert


---

## 一、准备环境

### 1. 创建练习文件夹

在 Trae 中打开一个新文件夹，例如：


D:\GitPractice


### 2. 打开终端

快捷键：


Ctrl + `


### 3. 检查 Git 是否安装成功

```bash
git --version
二、初始化仓库
git init
git status

📌 说明：

会生成 .git 文件夹
当前目录变成 Git 仓库
三、创建文件并查看状态

创建：

README.md

内容：

# Git Practice

这是我的 Git 练习项目。

查看状态：

git status

📌 输出：

Untracked files:
  README.md
四、添加到暂存区
git add README.md
# 或
git add .
git status

📌 输出：

Changes to be committed:
  new file: README.md
五、提交代码
git config --global user.name "NiTian"
git config --global user.email "2642791393@qq.com"

git commit -m "first commit"
六、查看历史记录
git log
git log --oneline
七、查看修改差异

修改 README.md 后：

git status
git diff

提交：

git add .
git commit -m "update README"
八、分支操作
查看分支
git branch
创建分支
git branch dev
切换分支
git switch dev
创建并切换
git switch -c feature-login
九、分支开发
git add .
git commit -m "add login feature"

切回主分支：

git switch main
十、合并分支
git merge feature-login

查看图：

git log --oneline --graph --all
十一、删除分支
git branch -d feature-login
十二、撤销修改
丢弃工作区修改
git restore README.md
撤销暂存
git restore --staged README.md
十三、版本回退
保留修改
git reset --soft HEAD~1
回到工作区
git reset HEAD~1
强制回退（危险⚠）
git reset --hard HEAD~1
十四、安全撤销（推荐）
git revert 提交ID

📌 特点：

不删除历史
生成反向提交
十五、临时保存修改
git stash
git stash list
git stash pop
十六、远程仓库操作
查看远程
git remote -v
添加远程仓库
git remote add origin 仓库地址
修改地址
git remote set-url origin 新地址
删除远程
git remote remove origin
十七、推送代码
git branch -M main
git push -u origin main

之后：

git push
十八、拉取代码
git pull
十九、克隆项目
git clone 仓库地址
cd 项目目录
二十、忽略文件

创建 .gitignore：

*.log
node_modules/
dist/
.idea/
.vscode/

提交：

git add .gitignore
git commit -m "add gitignore"
二十一、常用工作流（最重要 ⭐）
git status
git add .
git commit -m "说明"
git push

多人协作：

git pull
git add .
git commit -m "功能"
git push
二十二、学习顺序建议
第一遍（基础）
init / status / add / commit / log
第二遍（分支）
branch / switch / merge
第三遍（撤销）
restore / reset / revert
第四遍（远程）
remote / push / pull / clone
🎯 Git 核心思想总结
1️⃣ 保存版本
git add + git commit
2️⃣ 查看状态
git status / git log / git diff
3️⃣ 并行开发
git branch / git switch / git merge
4️⃣ 远程协作
git remote / git push / git pull / git clone